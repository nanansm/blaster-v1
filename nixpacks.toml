[phases.setup]
nixpkgs = "nixpkgs/nixos-25.05"
providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .next/standalone/server.js"