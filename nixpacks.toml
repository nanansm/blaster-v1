[phases.setup]
nixpkgs = "nixpkgs/nixos-23.11"
providers = ["node"]

# Paksa Nixpacks pakai Node.js 20
[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"