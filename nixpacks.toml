[phases.setup]
nixpkgs = "nixpkgs/nixos-23.11"
providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "20"
# ✅ Force disable Turbopack for build
NEXT_TURBOPACK = "0"

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"